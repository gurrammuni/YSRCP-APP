apiVersion: apps/v1
kind: Deployment
metadata:
  name: ysrcp-deployment
  namespace: default
  labels:
    app: ysrcp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ysrcp
  template:
    metadata:
      labels:
        app: ysrcp
    spec:
      containers:
      - name: ysrcp
        image: jithendragurram/ysrcp-app:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ysrcp-service
  namespace: default
  labels:
    app: ysrcp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: ysrcp
