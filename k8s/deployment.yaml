apiVersion: apps/v1
kind: Deployment
metadata:
  name: ysrcp-deployment
  namespace: default
  labels:
    app: YCP
spec:
  replicas: 3
  selector:
    matchLabels:
      app: YCP
  template:
    metadata:
      labels:
        app: YCP
    spec:
      containers:
      - name: YCP
        image: jithendragurram/YCP-app:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: YCP-service
  namespace: default
  labels:
    app: YCP
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: YCP
